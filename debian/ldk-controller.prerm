#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop ldk-controller.service >/dev/null 2>&1 || true
        systemctl disable ldk-controller.service >/dev/null 2>&1 || true
        ;;
esac

exit 0

